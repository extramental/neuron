port = 9000

debug = True
log_to_stderr = True

redis = {
    "host": "localhost",
    "port": 6379,
    "db": 0
}

from cerebro.support.neuron.auth import CerebroSessionAuthPolicy
auth_policy = CerebroSessionAuthPolicy

cerebro_config = "cerebro/development.ini"
